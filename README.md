# sonoran_callcommands
SonoranCAD Call Commands Plugin

## Install

- Copy folder to your `sonorancad/plugins` folder. 
- Modify configuration found in `config_callcommands.dist.lua` and copy to a `config_callcommands.lua` file.
